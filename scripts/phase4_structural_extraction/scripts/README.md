Phase 4 — Structural extraction pipeline: build the extraction manifest (D2/D3), capture JS-rendered pages with Playwright, auto-extract structural indicators from HTML, queue uncertain results for human review (D6 gate), apply review decisions, validate, and promote to `data/collection/`. Run in order: `build_extraction_manifest` → `browser_render_capture` → `extract_structural` → `build_review_queue` → (fill review_queue.csv) → `apply_review` → `validate_structural` → `promote_to_collection` → `summarize_structural_coverage`.
